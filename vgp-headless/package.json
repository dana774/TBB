{
  "name": "vgp-headless",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Value Growth Partners — headless institutional site (Astro + Wix headless data). Code-driven frontend mirroring the Brand Blueprint approach.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/node": "^8.3.4",
    "@astrojs/vercel": "^7.8.2",
    "astro": "^4.16.18"
  },
  "comment": "For live Wix CMS data, install the headless SDK separately: npm i @wix/sdk @wix/data (content.ts lazy-imports them; the build does not require them)."
}
