# CLAUDE.md

All agent prompts for this repo are indexed in AGENTS.md (imported below). `main` is the single
source of truth; start new work from it. Each prompt is also available as a slash command in
`.claude/commands/` (type `/` in any Claude app opened on this repo).

@AGENTS.md
